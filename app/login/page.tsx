import LoginClient from "../../components/LoginClient";

export const metadata = {
  title: "Sign in | AskSAV",
  description: "Sign in to your AskSAV AskSAV account.",
};

export default function LoginPage() {
  return <LoginClient />;
}
